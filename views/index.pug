extends layout

block content

  h1#game-name
    | Guess
    span Who?

    form(action='/add_guess', method='POST')
    p
      | Ready to make your guess? Type in your answer.
      input(type='text', name='guess', value='')
    input(type='submit', value='Submit')

    p
      | SCORE: 0 : 0

  div.game-container
    div#gameboard.game-item






  script.
    window.NAMESPACE = "#{namespace}";
